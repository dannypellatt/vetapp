@model IEnumerable<Pet>

<h2>Pets</h2>
<table class="table">
    <tr>
        <th>Pet ID</th>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Type</th>
    </tr>

    @foreach (var pet in @Model)
    {
        <tr>
            <td>@pet.idpet</td>
            <td>@pet.pet_first_name</td>
            <td>@pet.pet_last_name</td>
            <td>@pet.pet_type</td>
        </tr>
    }

</table>